{"id":"cp-sync-5mh","title":"Consider switching CCB events to event_profiles endpoint for full venue addresses","description":"## Context\nCurrently using `public_calendar_listing` endpoint which only provides venue name as a string. The `event_profiles` endpoint provides full location objects with complete address data.\n\n## Current State\n✅ Code handles both formats gracefully\n✅ Venues import with names from `public_calendar_listing`\n⚠️ No street address, city, state, zip data available\n\n## Potential Enhancement\nSwitch from `public_calendar_listing` to `event_profiles` endpoint to get:\n- venue name\n- street_address  \n- city\n- state\n- zip\n\n## Considerations\n**public_calendar_listing:**\n- ✅ Filters by date range (date_start, date_end)\n- ✅ Returns event occurrences\n- ❌ Location is string only (no address)\n\n**event_profiles:**\n- ✅ Full location object with addresses\n- ✅ Official documented endpoint\n- ⚠️ Filters by modified_since (not date ranges)\n- ⚠️ Returns master events (not occurrences)\n\n## Action Items\n- [ ] Evaluate if date-range vs modified_since filtering works for use case\n- [ ] Test event_profiles endpoint integration\n- [ ] Consider hybrid approach (fetch list from public_calendar_listing, get details from event_profile by ID)\n- [ ] Update admin settings to allow endpoint selection\n\n## Files to Modify\n- `includes/ChMS/CCB.php` - fetch_events() method (line ~631)\n\n## Note\nNo urgent action needed - venues are importing correctly with available data. This is an enhancement opportunity when full addresses are desired.","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T12:07:28.346397-06:00","created_by":"tannermoushey","updated_at":"2026-01-30T12:07:28.346397-06:00"}
{"id":"cp-sync-ijx","title":"Fix CCB event venue import to support location data","description":"## Problem\nCCB events were importing but venues had no location/address data.\n\n## Investigation\n- CCB API has two event endpoints:\n  - `public_calendar_listing`: Returns location as simple string (venue name only)\n  - `event_profiles`: Returns location as nested object with full address details\n\n## Solution\nUpdated location normalization in both `/includes/ChMS/CCB.php` and `/includes/ChMS/cli/CCB.php` to handle both formats:\n\n1. **String format** (public_calendar_listing): Extracts venue name\n2. **Object format** (event_profiles): Extracts name + street_address, city, state, zip\n\n## Changes Made\n- Updated `normalize_ccb_event()` to detect and handle both location formats\n- Enhanced `format_event()` to create venues with all available address data\n- Updated CLI commands to display location information properly\n- Added logging for venue creation\n\n## Result\n✅ Venues now import correctly with venue names\n✅ Code ready to support full addresses when using event_profiles endpoint\n✅ Gracefully handles both minimal and complete location data\n\n## Files Modified\n- `includes/ChMS/CCB.php`\n- `includes/ChMS/cli/CCB.php`","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-30T12:06:32.625841-06:00","created_by":"tannermoushey","updated_at":"2026-01-30T12:06:36.692647-06:00","closed_at":"2026-01-30T12:06:36.692647-06:00","close_reason":"Closed"}
{"id":"cp-sync-mol","title":"Handle CCB events without modified timestamps in enrichment logic","description":"## Problem\nThe CCB event enrichment logic assumes all events have `_chms_modified` timestamps to determine if enrichment is needed. Events without this field will cause issues.\n\n## Current Code Location\n`includes/ChMS/CCB.php` - `maybe_enrich_event_after_update()` method around line 980\n\n## Current Logic\n```php\n$stored_modified = get_post_meta( $post_id, '_chms_modified', true );\n$event_modified = $item['_chms_modified'] ?? '';\n\nif ( empty( $event_modified ) || $stored_modified === $event_modified ) {\n    return; // Skip enrichment\n}\n```\n\n## Issue\nIf `$item['_chms_modified']` is empty, we skip enrichment. This means events without modified timestamps never get enriched.\n\n## Potential Solutions\n\n1. **Always enrich on first import:** Check if `_chms_enriched_at` meta exists, enrich once if not set\n2. **Use creation date fallback:** Use `post_date` as fallback timestamp\n3. **Never enrich without timestamp:** Skip enrichment entirely (current behavior)\n4. **Force re-enrichment flag:** Add admin option to force re-enrichment of all events\n\n## Recommended Approach\nOption 1: Check for `_chms_enriched_at` meta. If not set, enrich once. This ensures all events get enriched at least once while avoiding redundant API calls.\n\n## Testing Needed\n- Import events from CCB API that lack modified timestamps\n- Verify enrichment behavior\n- Check that enrichment doesn't run repeatedly","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-31T13:40:56.348695-06:00","created_by":"tannermoushey","updated_at":"2026-01-31T13:40:56.348695-06:00"}
{"id":"cp-sync-u9o","title":"Prevent race condition in CCB venue creation during concurrent event imports","description":"## Problem\nMultiple events with the same venue name imported concurrently could create duplicate venue posts. Current code checks for existing venues by title but has no locking mechanism.\n\n## Current Code Location\n`includes/ChMS/CCB.php` - Two locations:\n1. `format_event()` method around line 850 (initial event import)\n2. `update_event_with_enriched_data()` method around line 1221 (enrichment)\n\n## Current Logic\n```php\n$existing_venue = get_posts( [\n    'post_type' =\u003e 'tribe_venue',\n    'title' =\u003e $venue_name,\n    'numberposts' =\u003e 1,\n] );\n\nif ( ! empty( $existing_venue ) ) {\n    $venue_id = $existing_venue[0]-\u003eID;\n} else {\n    // Create new venue\n    $venue_id = $tec-\u003ecreate_venue( $venue_args );\n}\n```\n\n## Race Condition Scenario\n1. Event A checks for \"Main Building\" venue - none found\n2. Event B checks for \"Main Building\" venue - none found (Event A hasn't created yet)\n3. Event A creates \"Main Building\" venue (ID: 123)\n4. Event B creates \"Main Building\" venue (ID: 456) - DUPLICATE!\n\n## Potential Solutions\n\n1. **Transient locking:** Use `set_transient` with venue name as key before creation\n2. **Database unique constraint:** Add unique index on post_title for tribe_venue (risky, requires DB migration)\n3. **Post-import deduplication:** Cleanup job that merges duplicate venues after import\n4. **Venue pre-creation:** Import all unique venues first, then link events\n5. **Venue name + address uniqueness:** Check both name AND address to identify duplicates\n\n## Recommended Approach\nOption 1: Transient locking with timeout. Simple, non-invasive, handles most concurrent cases.\n\n```php\n$lock_key = 'ccb_venue_creation_' . sanitize_title( $venue_name );\nif ( false === get_transient( $lock_key ) ) {\n    set_transient( $lock_key, time(), 30 ); // 30 second lock\n    \n    // Double-check after acquiring lock\n    $existing_venue = get_posts( [...] );\n    if ( empty( $existing_venue ) ) {\n        $venue_id = $tec-\u003ecreate_venue( $venue_args );\n    }\n    \n    delete_transient( $lock_key );\n}\n```\n\n## Additional Consideration\nMay also want a cleanup tool to find and merge existing duplicates created before this fix.\n\n## Testing Needed\n- Import multiple events with same venue name simultaneously\n- Verify only one venue post is created\n- Test transient expiration handling","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-31T13:40:57.337834-06:00","created_by":"tannermoushey","updated_at":"2026-01-31T13:40:57.337834-06:00"}
